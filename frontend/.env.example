# Base URL of the nginx-fronted backend (auth cookies are same-site as long
# as this shares a registrable domain with the page origin — localhost with
# any port counts as same-site, so this works for local dev as-is).
VITE_API_BASE_URL=http://localhost
